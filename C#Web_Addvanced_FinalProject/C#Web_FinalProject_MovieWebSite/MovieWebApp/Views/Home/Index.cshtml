<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MovieVerse - Explore Cinema</title>
    <style>
        /* General Styling */
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            color: #8b0000; /* Deep red for text */
            text-align: center;
            background: linear-gradient(to bottom, #fff7e6, #ffe4b5); /* Light beige gradient background */
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        /* Title Styling */
        h1 {
            font-family: 'Playfair Display', serif; /* Elegant serif font for title */
            font-size: 3.5rem;
            color: #8b0000; /* Deep red */
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
            margin: 0;
        }

        /* Subtitle Styling */
        p {
            font-family: 'Roboto', sans-serif; /* Clean sans-serif font for subtitle */
            font-size: 1.5rem;
            margin: 15px 0 0;
            color: #4b2e83; /* Dark purple for subtitle */
            max-width: 800px;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.2);
        }

        /* Footer */
        footer {
            position: absolute;
            bottom: 0;
            width: 100%;
            background: rgba(255, 255, 255, 0.9);
            color: #4b2e83;
            text-align: center;
            padding: 10px 0;
            font-family: 'Roboto', sans-serif;
        }

            footer a {
                color: #8b0000;
                text-decoration: none;
                font-weight: bold;
            }

                footer a:hover {
                    text-decoration: underline;
                }
    </style>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Roboto:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <h1>MovieVerse</h1>
    <p>Explore the most famous Movies, Actors, Directors, Filming Countries, and MovieOrShow Quotes</p>
</body>
</html>
